export class CityInfo{

    constructor(public name:string, public countryCode:string, public temperature:number, public feelsLike:number, public condition:string, public imagePath:string, public iconPath:string) {}
}